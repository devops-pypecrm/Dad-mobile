// Default entrypoint — mirrors `main_dev.dart` so plain `flutter run` works
// out of the box. Use `flutter run -t lib/main_dev.dart` /
// `-t lib/main_prod.dart` (or the `dev`/`prod` VS Code launch configs) to be
// explicit about which flavor you're targeting.
export 'main_dev.dart';
